{
  "$schema": "https://wails.io/schemas/config.v2.json",
  "//name": "Sets the .app bundle directory name AND the bundle identifier (com.wails.<name>). outputfilename sets the executable inside it. Both must be Hydra or the bundle is Hydra.app containing hydradesk, or vice versa.",
  "name": "Hydra",
  "outputfilename": "Hydra",
  "frontend:install": "npm install",
  "frontend:build": "npm run build",
  "frontend:dev:watcher": "npm run dev",
  "frontend:dev:serverUrl": "auto",
  "info": {
    "productName": "Hydra",
    "comments": "Local-first, multi-vendor AI control plane."
  }
}
